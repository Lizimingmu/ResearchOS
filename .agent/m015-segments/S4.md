# S4 — Chinese-first content workbench (OpenCode continuation)

Status: COMPLETED (implemented together with S2/S3 in one Stage-B pass; see S2.md for the full file list)
Timestamp: 2026-08-26
Executor: OpenCode / DeepSeek V4 Pro

Scope note: this report is a copy of the combined Stage-B report; the authoritative description of S4 work (内容库/草稿/待审核/发布箱/版本历史/冲突 views, detail editor, filters, validation panel, patch import UI, version compare/rollback UI) lives in `S2.md`. No scientific seed content was added.

## Files changed

- `src/domain/contentStudio.ts` — added `PersonalContentValidationReport`, `ParsedPatchPack`.
- `src/services/contentStudio.ts` — `resolveEffectiveContent` now resolves only `active && activeForLearning` overlays (draft/pending/archived/deprecated/superseded can never leak into learning, search or publishing); `KIND_TEMPLATES` structural placeholders (empty values only, no scientific text); `duplicatePersonalEntry`; `diffPayloads` deterministic field diffs; `validatePersonalContentDetailed` (errors/warnings/evidence gaps/dependency impact/entersLearning); total `parsePatchPackJson` (200 KB bound, structured failures); `previewPatch` refactored onto `diffPayloads`.
- `src/state/store.ts` — new actions `duplicatePersonalDraft`, `previewPersonalPatch` (records an open `stale_patch` conflict on expired base with zero mutation), private activation keeps `verificationStatus: pending`; lifecycle guard unchanged.
- `src/features/content-studio/ContentStudioView.tsx` — detail editor (title/risk/payload JSON/dependency keys) with live deterministic validation panel; kind filter + search; 复制 duplicate; 提交审核 / 私人启用（仍待核验）/ 归档 / 标记弃用 transitions; 版本比较与恢复 (snapshot vs current diff, reason input, rollback generates a new revision and returns content to 待审核); 修订包导入 panel (paste JSON → parse → dry-run field diff → stale/conflict gating → all-or-nothing apply); conflicts tab shows stale-patch conflicts.
- `src/styles/app.css` — filters/payload/diff styles.
- `tests-node/suite.mjs` — +6 tests (lifecycle leakage for five lifecycles + misconfigured active flag; detailed validation determinism/gaps/impact/self-verify block; duplication independence; total patch parsing incl. oversize; ordered deterministic diffs; store-level stale-conflict zero-mutation + atomic apply flow).

## Implemented behavior

- Built-in objects remain immutable (patches target personal entries only; preview reports 内置对象不可通过修订包修改).
- Old revisions remain resolvable; rollback appends rather than overwrites; historical snapshots keep payload copies.
- High-risk/AI-generated content stays pending; explicit private activation never relabels verification status (store + validation + UI strings).
- Draft/archive/deprecated/superseded items cannot appear in effective content used by any surface.

## Focused verification

- `npm run test:unit` → PASS 65/65 (was 59).

## Failures

One test fixture defect: direct-edit step did not change the payload hash so the stale-base precondition failed; fixed by providing a payload in the fixture. No product code changed.

## Next segment

S5 (handoff Stage C): curated Obsidian publisher.
