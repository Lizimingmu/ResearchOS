# S6 — Optional Obsidian review round trip (OpenCode continuation)

Status: COMPLETED
Timestamp: 2026-08-26 (background session)
Executor: OpenCode / DeepSeek V4 Pro

## Files changed

- `src/domain/contentStudio.ts` — new `ReviewBatchManifest` (notes carry export-time managed hashes); batch gained `reviewManifest` / `reviewCheckedAt`.
- `src/services/obsidianPublish.ts` — `buildReviewRoundTrip` (finite deterministic `_Review/<batch-id>/manifest.json` + listed notes with 审核意见 template), manifest-gated `parseReviewFeedback` (rejects extra/missing/identity-mismatched paths), `toReviewPatchCandidates` (annotation text imported verbatim as pending `{审核意见}` field; managed-block payload edits extracted mechanically), `extractPayloadFromManaged`.
- `src/state/store.ts` — `exportReviewRoundTrip` (explicit confirmed write of exactly the manifest plus its notes; manifest persisted on the batch) and `checkReviewRoundTrip` (reads the stored manifest back first, refuses on any mismatch, then reads only the listed note paths and returns feedback plus dry-run candidates).
- `src/features/content-studio/ContentStudioView.tsx` — 审核往返 panel: explicit selection, explicit confirmation checkbox, 导出审核批次, 检查 Obsidian 审核意见, per-candidate dry-run diffs, per-candidate apply that produces a 待审核 revision without status promotion.
- `tests-node/suite.mjs` — +3 tests.

## Implemented behavior

- No watcher, no automatic pull, no merge, no publish, no delete/cleanup, no status promotion anywhere in the round trip.
- Read-back is limited to the exact exported manifest contents; a changed manifest aborts before reading any note.
- All candidates are `pending_review` / `pending`; applying one keeps old revisions resolvable and never claims verification.

## Focused verification

- `npm run test:unit` → PASS 71/71 (3 new tests).

## Failures

Test-contract mismatch fixed twice: the read-back gate intentionally receives only note entries (the store verifies the manifest separately), so test fixtures that included the manifest entry were corrected to match the designed contract. No product code changed for these fixes. One real defect was found and fixed during this stage: `toReviewPatchCandidates` referenced a nonexistent lookup for file contents; `ReviewFeedbackEntry` now carries its contents explicitly.

## Next segment

S7 (handoff Stage E): full regression, deterministic audits, reports.
