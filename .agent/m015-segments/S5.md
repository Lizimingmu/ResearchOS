# S5 — Curated Obsidian publisher (OpenCode continuation)

Status: COMPLETED
Timestamp: 2026-08-26 (background session)
Executor: OpenCode / DeepSeek V4 Pro

## Files changed

- `src/domain/contentStudio.ts` — `ObsidianPublishItem` gained action/detail/previousContents; batch gained `selectedKeys`, `requiresSecondConfirmation`, `appliedFingerprints`, `batchType`; connection gained `validatedResolvedDir`; new `AppliedPublishFingerprint`.
- New `src/services/obsidianPublish.ts` — byte-preserving note parser, deterministic managed-body rendering (timestamp-free), frontmatter identity contract (`researchos_id/kind/revision/status/hash/published_at`), safe filename derivation, pure planner (`create/update/conflict/unchanged`, rename follows stable ID, 20-note default limit, deterministic confirmation token) and pure applier (all-or-nothing, user bytes outside the managed block preserved byte-for-byte).
- `src/services/desktop.ts` — read-only `validateObsidianTarget`/`listMarkdownFiles`/`readTextFiles` and confirmed-write bridge.
- `src/state/store.ts` — connection save (read-only validation first), publish batch create → preview → confirm+apply → cancel lifecycle with applied fingerprints persisted.
- `src/features/content-studio/ContentStudioView.tsx` — 发布箱: connection card (explicit 只读验证), selection list, exact-path preview table with per-item actions/details, conflict blocking, second-confirmation checkbox for >20 notes, atomic apply, history.
- `src-tauri/src/lib.rs` — dedicated-subfolder resolver that creates nothing during validation and rejects symlink/junction components plus `.obsidian`; read-only listing (skips dot-directories, 500 cap); contained read; confirmed writes with per-directory case-insensitive + decomposed-Unicode collision rejection, preview-time preconditions (expected bytes or must-not-exist) verified for the whole batch before any write, then atomic temp→backup→rename.
- `tests-node/suite.mjs` — +3 tests (note contract/determinism/honest pending status; plan lifecycle incl. rename-follows-ID, user-byte preservation, managed-edit conflict, foreign-note conflict; 20-limit/token/all-or-nothing).

## Implemented behavior

- Connection validation, listing and reading are strictly read-only; the dedicated folder is created only inside a confirmed transaction.
- Repeated unchanged republish writes nothing; title renames follow `researchos_id`; externally edited managed blocks produce conflicts with zero writes; manual text after the managed block survives updates byte-exactly.
- Pending content publishes with honest 待核验 labels and `researchos_status: user_pending`; builtin notes are marked `builtin`.

## Focused verification

- `npm run test:unit` → PASS 68/68 (3 new tests).
- `cargo test --manifest-path src-tauri/Cargo.toml` (offline single attempt) → PASS 15/15 (3 new tests incl. zero-write precondition/collision/idempotency proofs on isolated temp dirs).

## Failures

One Windows-specific test assertion error (case-insensitive path resolution made a "no extra file" check meaningless); replaced with a directory-entry count. One earlier fixture mistake in Stage C planning test (filename sanitizer expectation). No product logic changed for either fix. A mid-edit structural slip in `write_confirmed_files` was caught by `cargo check` and rewritten before any green claim.

## Next segment

S6 (handoff Stage D): optional finite `_Review/<batch-id>` round trip.
