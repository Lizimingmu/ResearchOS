# S1 — Unified content contract and review-pack export (OpenCode continuation)

Status: COMPLETED
Timestamp: 2026-08-26 (background session, resumed from checkpoint `7c65c9d`)
Executor: OpenCode / DeepSeek V4 Pro

## Files read

`.agent/OPENCODE_HANDOFF.md`, `.agent/TESTING_POLICY.md`, `.agent/PRODUCT_CONSTITUTION.md`, `.agent/SCIENTIFIC_GATES.md`, `.agent/M015_PERSONAL_CONTENT_STUDIO_PLAN.md`, `.agent/M015_RUN_STATE.json`, `AGENTS.md`, `PROJECT_STATE.md`, `CURRENT_MILESTONE.md`, existing M015 core sources (`src/domain/contentStudio.ts`, `src/services/contentStudio.ts`, `src/services/contentInventory.ts`, `src/state/store.ts`, `src/state/migrations.ts`, `src/features/content-studio/ContentStudioView.tsx`, `src/services/desktop.ts`, `src-tauri/src/lib.rs`), `tests-node/suite.mjs`.

## Files changed

- Added `src/services/safePaths.ts` — deterministic relative-path gate (traversal/absolute/UNC/drive/`.obsidian`/control chars/reserved names/depth+length bounds) and export-destination validation that rejects destinations inside a configured vault root.
- Extended `src/services/contentStudio.ts` — `auditContentInventory` (duplicates, SHA-256 payload-hash mismatches, provenance gaps, missing dependencies, deterministic cycle detection) and `reviewPackFileEntries` (exactly `manifest.json`, `content.json`, `REVIEW_COPY.md`, `SCIENTIFIC_CHANGESET.md`, `dependencies.json`).
- Extended `src-tauri/src/lib.rs` — safe filesystem gateway: pure relative-path validation, resolved-containment proof via canonicalization (defeats traversal and symlink/junction escape), atomic temp→backup→rename replacement with restore-on-failure, byte-identical skip; new command `export_review_pack`; 5 new Rust tests.
- Extended `src/services/desktop.ts` — `exportFiles` bridge.
- Extended `src/features/content-studio/ContentStudioView.tsx` — 内容库 multi-select + 导出审核包 button (user-triggered; destination picker warns to stay outside the vault; destination validated against configured vault root; failures notify with zero writes).
- `src/styles/app.css` — content-studio styles.
- `tests-node/suite.mjs` — +3 tests (inventory audit determinism/defects, exact-five review-pack files, unsafe-path/destination gates); workbench render assertions extended.

## Implemented behavior

- Unknown IDs, dependency cycles, duplicates, missing evidence and unsafe paths fail before any write; export writes only after full batch validation, atomically, and skips byte-identical targets.
- Review pack contains exactly the five required files with deterministic bytes.
- No scientific text or verification status changed anywhere.

## Focused verification

- `npm run test:unit` → PASS 59/59 (was 56).
- `cargo test --manifest-path src-tauri/Cargo.toml` (offline, `CARGO_HOME=<repo>\.cargo-home`, single attempt) → PASS 12/12 (7 pre-existing + 5 new).

## Failures

Two compile-fix cycles in new Rust test code only (`\u{7}` escape; wrong type annotation; one invalid fixture `"bad name .txt"` replaced by a real trailing-space case). No product logic changed during fixes; final run green.

## Next segment

S2/S3/S4 (handoff Stage B): personal maintenance editors, patch UI, lifecycle leakage guards.
