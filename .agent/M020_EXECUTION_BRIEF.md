# M020 implementation brief

> Historical dispatch brief. OpenCode failed before implementation (log permissions; then EPERM spawning git). Codex collaborators completed the work; current results and stop instruction are in IMPLEMENTATION_REPORT.md and CURRENT_MILESTONE.md.

User explicitly replaced the unfinished independent audit with M020 development. The complete current request is `.agent/M020_USER_REQUIREMENTS.md`; it overrides older milestone/handoff scope restrictions. Implement that request fully. Codex owns Git; do not execute any Git command. Current baseline is `7a41b4926e3a2a408acce7c073b2351c4fc230cc`; Codex has created `codex/m020-extensible-knowledge-architecture`.

OpenCode / configured DeepSeek V4 Pro is the implementer. Read AGENTS.md and its required entry files, then this brief and the user requirements. Preserve existing scientific prose, keys, Learning Kernel, M019 assessment behavior and all pending statuses. Do not fix issues from the preceding audit. Do not activate content. Do not introduce dependencies unless necessary. Background-only verification; localhost/browser permission was denied and must not be bypassed. No foreground app/installer.

## Implementation expectations

- Add a typed, hash-bound, immutable revision Knowledge Unit contract for all six types, with common scientific fields plus actual type-specific validation, freshness and status policy. Missing migrated fields are explicit review gaps, never guessed scientific content. Canonical records coexist with existing learning contracts; projections bind exact knowledge id/revision/hash.
- Reuse existing source ingestion, Content Studio revision/diff/overlay machinery where feasible. Adapters must cover existing active and staged concepts/methods and actual Protocol staging JSON (WB, qPCR, IHC, flow, PDO); preserve source IDs, original payload/provenance/revisions and pending state. Avoid shipping the whole legacy corpus if an on-demand import adapter suffices, but make the built-in staging import workflow usable without writing schema.
- Dependency graph must track source -> claim -> knowledge -> all learning kinds, including cases, assessment and protocol. Do not call source-to-all-content scans a precise claim graph. Preserve explicit unresolved links as review required; deterministic dangling/cycle/orphan audit. Changes produce exact impact previews and patch proposals, not silent changes to learning content.
- Keep revision payloads immutable. Head/lifecycle/supersession decisions can use separate append-only records. A human-confirmed guideline supersession transaction makes the old revision unavailable for new learning while new revision remains pending; it must not publish the new revision. Do not let an arbitrary AI import silently invalidate trusted content without an explicit update action. Source metadata verification never verifies claims or knowledge.
- Add a persistent additive schema migration (future schema rejection tests must move forward with the new schema). Existing learning events retain existing asset id/revision/hash; keep a deterministic historical binding resolver for the known original asset revisions and record exact knowledge bindings for new events. Never infer competence from migration.
- Wire maintenance holds into actual Today/Review and submission boundaries for affected content. This is a safety gate around existing kernel behavior, not a kernel refactor. Untouched lessons retain behavior; pending new candidates cannot enter standard competence. No UI-only safety controls.
- Chinese form-based Add Knowledge / templates for concept, method, protocol, guideline, research pattern, experimental technique. Support create, duplicate, revise, supersede, deprecate, restore, compare revisions with clear pending review provenance. DOI/PMID/metadata, source pack, Markdown, JSON, notes must produce reviewed-previewable candidates, not require raw schema for ordinary users. No automatic network requests.
- Candidate learning projections (Guide/Concept/Method/Protocol/Apply/Remediation/Delayed Review/Case) are pending, have source bindings, and carry explicit incomplete fields when they cannot be derived safely. Do not fabricate scientifically complete lessons or answer keys. Show proposal diff/impact; no activation action this milestone.
- Simple Knowledge Maintenance tab integrated into existing Content Studio (discoverable from existing app) for Updates, Review Required, Superseded, Emerging, Conflicts. Persist operations, candidates, revision history and impact review state. Stale revisions/overlay conflicts fail closed. Avoid a disconnected demo screen.
- Seven requested deterministic audits must execute independently and fail for malformed fixtures, not merely count fields. Test actual four acceptance scenarios plus tampering, stale candidate bases, supersession consistency, source/claim distinction, history resolution, malformed import, state roundtrip and activation rejection. Include privacy/no-network checks.
- Run typecheck and meaningful tests, then full tests. Write `.agent/IMPLEMENTATION_REPORT.md`, `.agent/SCIENTIFIC_CHANGESET.md` (state no scientific content edits if true), and `M020_EXTENSIBLE_KNOWLEDGE_ARCHITECTURE_REPORT.md`. Report unimplemented portions honestly. Never claim UI was visually verified. Codex will independently review, run final checks, commit and push.

Complete implementation and stop for Codex review. Do not start M021.
