# ResearchOS 试用指南 (Pilot Start Here)

欢迎使用 **ResearchOS Pilot**！

本版本是专为 30–60 分钟真实科研学习体验准备的本地试用版本。无需配置任何 API Key，无需复杂的编译环境，所有数据完全在本地运行与保存。

---

## 1. 快速启动

在 Windows 电脑上：

1. 打开 ResearchOS 根目录：`D:\Agents\ResearchOS`
2. **双击运行 `START_RESEARCHOS_PILOT.bat`**
3. 脚本会自动完成构建并拉起默认浏览器，打开 `http://localhost:5173`

> 如果遇到端口占用或想在终端手动启动：
> ```bash
> npm run build
> node scripts/serve.mjs
> ```
> 并在浏览器访问 `http://localhost:1420` 或终端提示的地址。

---

## 2. 核心试用流程 (推荐 30–45 分钟)

### 步骤一：一键开启试用
在启动首页直接点击绿色的 **「开始试用 (Start Pilot)」** 按钮，系统会自动配置好今天最适合科研训练的基础路径。

### 步骤二：进入今日学习 (Today 视图)
进入系统后，您将看到精简的今日学习面板：
* **Foundation Thread（基础主线）**：推荐先从核心概念开始，例如 **「统计推断单元 (Statistical Unit)」**。
* **清晰的“为什么今天学这个”说明**：每项任务均标明先修关系与训练意图。

### 步骤三：体验完整学习闭环
点击进入课程后，按照经典学习链条沉浸体验：
$$\text{Why (动机)} \rightarrow \text{Intuition (直觉模型)} \rightarrow \text{Predict (预测思考)} \rightarrow \text{Explain (自主解释)} \rightarrow \text{Worked Example (范例)} \rightarrow \text{Boundary (边界辨析)} \rightarrow \text{Apply (独立判断)}$$

* **Explain 阶段亮点**：请在输入框用您自己的话尝试解释该概念，并勾选 4 项自检清单。
* **Apply 阶段**：系统会呈现严谨的真实科研场景选择题，帮助您验证是否真正掌握。

### 步骤四：完成反馈
* 每次完成一节课程，系统会自动弹出 **「试用体验反馈」** 卡片。
* 您可以快速评价难度（偏难 / 刚好 / 偏简单）、勾选遇到的问题（如题目有歧义、讲解不够直观等），并写下任何自由评价。
* 即使中途跳过，也可在页面右上角点击 **「试用反馈」** 按钮随时补充。

### 步骤五：查看与导出反馈
* 点击顶栏右上角 **「试用反馈」** 进入反馈管理面板。
* 查看本次试用提交的所有评价卡片。
* 点击 **「导出反馈报告 (JSON)」** 按钮，即可将本次试用记录保存为 `ResearchOS_Pilot_Feedback_YYYYMMDD.json` 文件发送给团队。

---

## 3. 安全重置与多用户试用

* 如果想重新开始或让下一位研究者试用：
  * 在「试用反馈」面板底部点击 **「开始新的 Pilot Session」**。
  * 该操作仅清理试用会话的进度与反馈，**绝不会删除或破坏** 本地系统知识库与用户项目数据。
* 试用期间随时关闭浏览器或终端，所有进度均持久保存在浏览器本地，重新打开后无缝恢复。

---

## 4. 您不需要关注的技术细节

* 内部的 406 个规范知识单元（KnowledgeUnit）、版本哈希溯源、盲审机制等底层系统已在后台严密保证运行安全。
* 目前存在争议的判定题目已被系统安全隔离，不会污染您的学习能力评价。
* 请放心地专注于概念理解、题目推理和使用流畅度体验！
