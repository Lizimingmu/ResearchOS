import { Beaker, BrainCircuit, ClipboardCheck, FileSearch, NotebookTabs, SearchCheck, Sparkles } from "lucide-react";
import { useAppStore } from "../../state/store";
const tools=[["case-lab","Case Lab","证据逐层揭示；锁定判断并查看 Decision Timeline",SearchCheck],["problem-atlas","科研问题诊断","从现象、候选原因和证据开始结构化诊断",SearchCheck],["methods","方法与概念","按需查阅已经核验的方法学内容",Beaker],["paper-lab","Paper Studio","渐进式重建问题、设计、证据任务和主张边界",FileSearch],["ai-audit","AI 分析审查","在接受 AI 建议前逐步检查风险",BrainCircuit],["assessment","能力评估","主动进行陌生案例评估，而不是首次强制盲测",ClipboardCheck],["frontier","前沿探索","把新兴方向与已证明能力明确分开",Sparkles],["content-studio","内容工作台","维护、审核和版本化个人学习内容",NotebookTabs]] as const;
export function PracticeHubView(){const setView=useAppStore((s)=>s.setView);return <div className="page practice-hub"><header className="page-header"><div><span className="eyebrow">Practice · Tools</span><h1>把知识用于科研判断</h1><p>完整工具仍然保留，但不再要求新用户从十几个 workspace 中猜入口。</p></div></header><div className="practice-tool-grid">{tools.map(([id,title,body,Icon])=><button key={id} onClick={()=>setView(id)}><Icon/><strong>{title}</strong><span>{body}</span></button>)}</div></div>}
