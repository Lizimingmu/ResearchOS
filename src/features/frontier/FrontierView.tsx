import { CircleDot, FileText, FlaskConical, Network, NotebookPen } from "lucide-react";
import { frontierNodes } from "../../data/examplePapers";

export function FrontierView() {
  const root = frontierNodes[0];
  return <div className="page frontier-page"><header className="page-header"><div><span className="eyebrow">FRONTIER MAP · FOUNDATION</span><h1>Question-centered landscape</h1><p>Nodes hold papers, methods, notes, questions, and projects. Pending nodes are never treated as facts.</p></div><span className="status-pill pending">Framework only · no auto-radar</span></header><div className="frontier-workspace"><section className="frontier-tree"><div className="frontier-root"><Network size={18} /><strong>{root.label}</strong><span>{root.status}</span></div>{root.children.map((node) => <article key={node.id}><span className="tree-line" /><CircleDot size={14} /><div><strong>{node.label}</strong><small>Familiarity {node.familiarity}/4 · {node.status}</small></div><span className={`status-pill ${node.status}`}>{node.status}</span></article>)}</section><aside className="frontier-inspector"><span className="eyebrow">NODE ATTACHMENTS</span><h2>RET biology</h2><div className="attachment-row"><FileText size={15} /><span>Papers</span><b>0</b></div><div className="attachment-row"><FlaskConical size={15} /><span>Methods</span><b>1</b></div><div className="attachment-row"><NotebookPen size={15} /><span>Notes</span><b>0</b></div><section className="frontier-question"><span>KEY QUESTION</span><p>Which resistance mechanisms are established in human tumors versus inferred from preclinical models?</p><small>verification_pending</small></section><p className="generated-warning">Unverified frontier prompts are structural aids. Verify citations before promoting content to knowledge.</p></aside></div></div>;
}

